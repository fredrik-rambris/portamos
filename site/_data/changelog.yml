# Auto-generated by scripts/generate_changelog.py — do not edit manually.
- tag: "v0.0.1-beta.22"
  changes:
    - "Better defaults for screen header in PacPicImporter."
    - "Document PacPic optimizer"
- tag: "v0.0.1-beta.21"
  changes:
    - "Fix native Windows build problems"
- tag: "v0.0.1-beta.20"
  changes:
    - "Fix native build problems"
- tag: "v0.0.1-beta.19"
  changes:
    - "Bank Set (.Abs) support — disasm/asm now handle .Abs bank set files containing multiple banks. New concat command bundles individual .Abk files into a .Abs bank set."
    - "PacPic screen header — Exported JSON now always includes a screen section, allowing palette, resolution, and screen dimensions to be set before reassembly."
    - "PacPic palette optimizer — New --optimize flag runs a greedy pairwise colour-swap search to minimise compressed output size. Can also be set per-bank via \"optimize\": true in the JSON."
    - "PacPic chip RAM default — PacPic banks now correctly default to fast RAM (chipRam: false)."
    - "CMAP fallback on import — When the JSON palette is all zeros, the importer automatically uses the palette embedded in the source image."
- tag: "v0.0.1-beta.18"
  changes:
    - "Correctly name 8SVX in options"
    - "Sprites are now laid out in rows and columns"
    - "Bugs fixed in SpriteBankImporter"
